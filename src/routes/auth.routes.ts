import { Router } from "express"
import { body } from "express-validator"

const router = Router()
// signUp-router
router.post("/signup",)
// signIn-router
router.post("/login",)
// signOut-router
router.post("/logout")

export default router